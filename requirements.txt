# SAT runtime dependencies. Pin loose enough for forward compatibility with
# new Qwen3 / LLaMA checkpoints, strict enough to avoid breaking changes.
torch>=2.1,<3.0
transformers>=4.44,<5.0
accelerate>=0.30
numpy>=1.24,<3.0
pyyaml>=6.0
sentencepiece>=0.1.99
tokenizers>=0.19
tqdm>=4.66
